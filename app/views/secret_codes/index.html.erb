<h1>Secret Codes</h1>

<table>
  <thead>
    <tr>
      <th colspan="3">Code</th>
      <th colspan="3">Associated To</th>
    </tr>
  </thead>

  <tbody>
    <% @secret_codes.each do |secret_code| %>

      <tr>
        <td><%=secret_code.code%></td>
        <td><%=secret_code.user.email if secret_code.user.present?%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Genrate Codes', new_secret_code_path %>